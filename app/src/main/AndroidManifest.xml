<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_group8"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_group8_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.A6Group8"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Messaging.MessagingChatActivity"
            android:exported="false" />
        <activity
            android:name=".AboutUsActivity"
            android:exported="false" />
        <activity
            android:name=".Weather.Search.SearchSuggestions"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".Weather.Search.SearchSuggestionsActivity" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_search_suggestions" -->
        <!-- android:theme="@style/Theme.A6Group8" /> -->
        <activity
            android:name=".Weather.WebServiceActivity"
            android:exported="false" />
        <activity
            android:name=".Weather.DetailedView.WeatherForecastDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".Messaging.MessagingActivity"
            android:exported="false" />
        <activity
            android:name=".Messaging.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>